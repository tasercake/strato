[project]
name = "strato"
version = "0.1.0"
dependencies = [
    "typer>=0.12.3",
    "tree-sitter",
    "tree-sitter-python @ git+https://github.com/tree-sitter/tree-sitter-python",
]

[project.scripts]
strato = "strato.cli:app"
