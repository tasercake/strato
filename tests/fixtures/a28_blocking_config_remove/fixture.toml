id = "A28"
name = "Blocking Config Remove Default"
source_files = ["main.py"]
config_files = ["pyproject.toml"]

[[runs]]
name = "default"
purpose = "proves the built-in database flags time.sleep before the remove override"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
