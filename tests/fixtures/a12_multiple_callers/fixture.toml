id = "A12"
name = "Multiple Callers"
source_files = ["main.py"]
config_files = ["pyproject.toml"]

[run]
name = "async-boundary"
purpose = "emits one diagnostic per async caller reaching the same blocking helper"
args = ["check", ".", "--output", "json"]
cache = "disabled"
