id = "A7"
name = "Non Blocking Override"
source_files = ["main.py"]
config_files = []

[run]
name = "default"
purpose = "lets @non_blocking suppress blocking propagation for the annotated function"
args = ["check", ".", "--output", "json"]
cache = "disabled"
