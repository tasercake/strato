id = "A40"
name = "Python 3.10 To Thread"
source_files = ["main.py"]
config_files = ["pyproject.toml"]

[run]
name = "python-310-to-thread"
purpose = "treats python_version 3.10 as supporting asyncio.to_thread"
args = ["check", ".", "--output", "json"]
cache = "disabled"
