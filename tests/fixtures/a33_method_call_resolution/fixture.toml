id = "A33"
name = "Method Call Resolution"
source_files = ["main.py"]
config_files = []

[[runs]]
name = "default"
purpose = "resolves instance, static, and class method calls through the ty facade"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
expected_exit_code = 1

[runs.expectation]
mode = "partial_json"
path = "expected.json"
assert = ["diagnostics"]
