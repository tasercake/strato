id = "A35"
name = "Dunder Operations"
source_files = ["main.py"]
config_files = []

[[runs]]
name = "default"
purpose = "classifies representative implicit dunder operations as STRATO004"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
expected_exit_code = 1

[runs.expectation]
mode = "partial_json"
path = "expected.json"
assert = ["diagnostics"]
