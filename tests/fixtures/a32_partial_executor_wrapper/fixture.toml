id = "A32"
name = "Partial Executor Wrapper"
source_files = ["main.py"]
config_files = []

[run]
name = "default"
purpose = "suppresses blocking propagation through functools.partial passed to run_in_executor"
args = ["check", ".", "--output", "json"]
cache = "disabled"
