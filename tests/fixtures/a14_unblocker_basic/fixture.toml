id = "A14"
name = "Unblocker Basic"
source_files = ["main.py"]
config_files = []

[[runs]]
name = "default"
purpose = "treats @unblocker calls as safe and still reports unwrapped direct blocking calls"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
expected_exit_code = 1

[runs.expectation]
mode = "partial_json"
path = "expected.json"
assert = ["diagnostics"]
