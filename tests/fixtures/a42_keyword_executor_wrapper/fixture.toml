id = "A42"
name = "Keyword Executor Wrapper"
source_files = ["main.py", "mylib.py"]
config_files = ["pyproject.toml"]

[run]
name = "keyword-callable-param"
purpose = "configured executor wrappers protect callable arguments passed by keyword"
args = ["check", ".", "--output", "json"]
cache = "disabled"
