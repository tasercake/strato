id = "A1"
name = "Direct Blocking"
source_files = ["main.py"]
config_files = []

[run]
name = "default"
purpose = "detects a direct known blocking call in an async function"
args = ["check", ".", "--output", "json"]
cache = "disabled"
