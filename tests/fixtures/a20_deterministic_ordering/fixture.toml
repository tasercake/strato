id = "A20"
name = "Deterministic Ordering"
source_files = ["main.py"]
config_files = []

[[runs]]
name = "first"
purpose = "orders diagnostics deterministically by source location on the first run"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
