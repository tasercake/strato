id = "A20"
name = "Deterministic Ordering"
source_files = ["main.py"]
config_files = []

[[runs]]
name = "default"
purpose = "orders diagnostics deterministically by source location"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
expected_exit_code = 1

[runs.expectation]
mode = "full_json"
path = "expected.json"
assert = ["diagnostics", "warnings", "stats"]
