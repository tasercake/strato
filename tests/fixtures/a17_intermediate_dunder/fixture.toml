id = "A17"
name = "Intermediate Dunder"
source_files = ["main.py"]
config_files = []

[[runs]]
name = "default"
purpose = "keeps STRATO004 classification when dunder invocation appears below an intermediate helper"
args = ["check", ".", "--output", "json"]
config = "defaults"
cache = "disabled"
expected_exit_code = 1

[runs.expectation]
mode = "partial_json"
path = "expected.json"
assert = ["diagnostics"]
